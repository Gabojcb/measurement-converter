/************
Processor: ts
************/

// index.ts
declare namespace ns_0 {
	/***
	 * Executes a useEffect hook binging the event defined in all
	 * objects passed
	 *
	 * @param {array} objects Objects to bind
	 * @param {function} onBinder function to be executed when the event is fired
	 * @param {string} event the event to be listened, by default is event change
	 */
	export function useBinder(objects: any[], onBinder: () => void, event?: string): void;
}

// use-texts.ts
declare namespace ns_1 {
	export function useTexts(specifier: string, key?: string): [boolean, object];
}

export import useBinder = ns_0.useBinder;
export import useTexts = ns_1.useTexts;

export declare const hmr: { on: (event: string, listener: any) => void; off: (event: string, listener: any) => void };
